<div class="productpage">
  <div class="productpage__product-care-guide-modal-overlay productpage__product-care-guide-modal-overlay--hidden">
      <div class="productpage__product-care-guide-modal">
        <div class="productpage__product-care-guide-modal-close-icon-container">
          <button class="productpage__product-care-guide-modal-close-button">
            {{ 'close-icon.png' | asset_url | img_tag: 'Close', 'productpage__product-care-guide-modal-close-icon' }}
          </button>
        </div>
        <div class="productpage__product-care-guide-modal-container">
          <h2 class="productpage__product-care-guide-title"> Lens Care</h2>
          <p class="productpage__product-care-guide-text">Your glasses open up your eyes to the world... so take care of them! Not sure how? We've got a few simple guidelines you can follow to care for your handcrafted frames and lenses.</p>
          <h4 class="productpage__product-care-guide-subTitle">Cellulose Acetate (Plastic) Frames</h4>
          <p class="productpage__product-care-guide-text">Over time, oxidation — evidenced by a cloudy, white build-up — may occur on your cellulose acetate frames. This reduces the beauty of your frame and can also cause the plastic to become more brittle. You can delay the onset of oxidation by periodically cleaning your frame with warm, soapy water. Avoid soaps that are scented or contain dyes. After cleaning, make sure to completely dry your frame. Under no circumstances should you clean your frame with chemical cleaners. If your frame begins to lose its glossy, luminous finish, please contact Selima Optique about our frame buffing services.
          <br/><br/>
          It's also common for cellulose acetate frames to become slightly stretched or loose over time. The simplest way to help your glasses keep their shape is to avoid placing them on top of your head and to use two hands to take your glasses on and off. Should your frames become loose, bring them to your nearest Selima Optique for an adjustment. Never adjust your frames on your own!</p>
        </div>
    </div>
  </div>
  <div class="productpage__product-information-image-container">
    {% assign current_variant = product.selected_or_first_available_variant %}
    {% assign featured_image = current_variant.featured_image | default: product.featured_image %}
    <div class="productpage__product-information-container">
      <div class="productpage__product-back-link-container">
        <a href="{{collection.url}}" class="'productpage__product-back-link-icon'"> {{ 'arrow-left.png' | asset_url | img_tag: 'Back' }}</a>
        <a class="productpage__product-back-link" href="{{collection.url}}">Back</a>
      </div>
      <div class="productpage__product-title-container">
        <h1 class="productpage__product-title">{{ product.title }}</h1>
        <h2 class="productpage__variant-title">{{current_variant.title}}</h2>
        <h3 class="productpage__variant-price">{{current_variant.price | money}}</h3>
          <div class="productpage__product-image-container-mobile">
        <div class="productpage__primary-product-image-container">
          <img src="{{ featured_image | img_url: 'large' }}" alt="{{ featured_image.alt | escape }}" class="productpage__primary-image">
            {{ 'bird.png' | asset_url | img_tag: 'Product Bird Accent', 'productpage__primary-image-accent productpage__primary-image-accent--hidden' }}
            {{ 'butterfly.png' | asset_url | img_tag: 'Product Butterfly Accent', 'productpage__primary-image-accent productpage__primary-image-accent--hidden' }}
            {{ 'butterfly2.png' | asset_url | img_tag: 'Product Butterfly Accent 2', 'productpage__primary-image-accent productpage__primary-image-accent--hidden' }}
            {{ 'flower-bird.png' | asset_url | img_tag: 'Product Flower Bird Accent', 'productpage__primary-image-accent productpage__primary-image-accent--hidden' }}
            {{ 'nature-clouds.png' | asset_url | img_tag: 'Product Nature Clouds Accent', 'productpage__primary-image-accent productpage__primary-image-accent--cloud productpage__primary-image-accent--hidden' }}
        </div>
        <div class="productpage__secondary-product-image-row-container">
          {% for variant in product.variants %}
            {% if variant.id != current_variant.id %}
              <button class="productpage__secondary-product-button" id="secondaryProductButton-{{variant.id}}">
                <img src="{{ variant.image | img_url: '152x76' }}" alt="{{ image.alt | escape }}" class="productpage__secondary-image" id="secondaryProductImage-{{variant.id}}">
                <span class="productpage__secondary-product-title" id="secondaryProductTitle-{{variant.id}}">{{ variant.title }}</span>
              </button>
            {% else %}
              <div class="productpage__secondary-product-image-container" id="secondaryProductButton-{{variant.id}}">
                <img src="{{ variant.image | img_url: '152x76' }}" alt="{{ image.alt | escape }}" class="productpage__secondary-image--selected" id="secondaryProductImage-{{variant.id}}">
                <span class="productpage__secondary-product-title--selected" id="secondaryProductTitle-{{variant.id}}">{{ variant.title }}</span>
              </div>
            {% endif %}
          {% endfor %}
        </div>
    </div>
        <button class="productpage__purchase-button" id="addToCart-{{current_variant.id}}">I'll Take Them!</button>
      </div>
      <div class="productpage__product-description-container">
        <p class="productpage__description">{{product.description}} </p>
        <p class="productpage__free-shipping-message">Free Shipping both ways in the USA </p>
      </div>
      <div class="productpage__product-care-guide-container">
        <button class="productpage__product-care-guide-button">Care Guide</button>
        {{ 'arrow-right.png' | asset_url | img_tag: 'Care Guide', 'productpage__product-care-guide-button-icon' }}
      </div>
    </div>
    <div class="productpage__product-image-container">
        <div class="productpage__primary-product-image-container">
          <img src="{{ featured_image | img_url: 'large' }}" alt="{{ featured_image.alt | escape }}" class="productpage__primary-image">
            {{ 'bird.png' | asset_url | img_tag: 'Product Bird Accent', 'productpage__primary-image-accent productpage__primary-image-accent--hidden' }}
            {{ 'butterfly.png' | asset_url | img_tag: 'Product Butterfly Accent', 'productpage__primary-image-accent productpage__primary-image-accent--hidden' }}
            {{ 'butterfly2.png' | asset_url | img_tag: 'Product Butterfly Accent 2', 'productpage__primary-image-accent productpage__primary-image-accent--hidden' }}
            {{ 'flower-bird.png' | asset_url | img_tag: 'Product Flower Bird Accent', 'productpage__primary-image-accent productpage__primary-image-accent--hidden' }}
            {{ 'nature-clouds.png' | asset_url | img_tag: 'Product Nature Clouds Accent', 'productpage__primary-image-accent productpage__primary-image-accent--cloud productpage__primary-image-accent--hidden' }}
        </div>
        <div class="productpage__secondary-product-image-row-container">
          {% for variant in product.variants %}
            {% if variant.id != current_variant.id %}
              <button class="productpage__secondary-product-button" id="secondaryProductButton-{{variant.id}}">
                <img src="{{ variant.image | img_url: '152x76' }}" alt="{{ image.alt | escape }}" class="productpage__secondary-image" id="secondaryProductImage-{{variant.id}}">
                <span class="productpage__secondary-product-title" id="secondaryProductTitle-{{variant.id}}">{{ variant.title }}</span>
              </button>
            {% else %}
              <div class="productpage__secondary-product-image-container" id="secondaryProductButton-{{variant.id}}">
                <img src="{{ variant.image | img_url: '152x76' }}" alt="{{ image.alt | escape }}" class="productpage__secondary-image--selected" id="secondaryProductImage-{{variant.id}}">
                <span class="productpage__secondary-product-title--selected" id="secondaryProductTitle-{{variant.id}}">{{ variant.title }}</span>
              </div>
            {% endif %}
          {% endfor %}
        </div>
    </div>
  </div>
  
  {% comment %} {% assign current_variant = product.selected_or_first_available_variant %}
  {% assign featured_image = current_variant.featured_image | default: product.featured_image %}
  <img src="{{ featured_image | img_url: 'large' }}" alt="{{ featured_image.alt | escape }}" id="ProductPhotoImg">
  {% for image in product.images %}
    <a href="{{ image.src | img_url: 'large' }}">
      <img src="{{ image.src | img_url: 'compact' }}" alt="{{ image.alt | escape }}">
    </a>
  {% endfor %}
  <h1>{{ product.title }}</h1>
  <form action="/cart/add" method="post" enctype="multipart/form-data" id="AddToCartForm">
    <select name="id" id="productSelect">
      {% for variant in product.variants %}
        {% if variant.available %}
          <option value="{{ variant.id }}">
            {{ variant.title }} - {{ variant.price | money_with_currency }}
          </option>
        {% else %}
          <option disabled="disabled">
            {{ variant.title }} - sold out
          </option>
        {% endif %}
      {% endfor %}
    </select>
    {{ current_variant.price | money }}
    <label for="Quantity">quantity</label>
    <input type="number" id="Quantity" name="quantity" value="1" min="1">
    <button type="submit" name="add" id="AddToCart">Add to cart</button>
  </form>
  <div>{{ product.description }}</div> {% endcomment %}
  <div class="productpage__media-container">
    <h4 class="productpage__media-title">Media</h4>
    <div class="productpage__media-image-container">
      {% for image in product.images %}
        {% unless image.attached_to_variant?%}
          {%if image.width == 100 %}
            <button class="productpage__media-image-button" id="mediaPageImageButton-{{image.alt}}-{{product.handle}}">
              <img class="productpage__media-image" id="productPageMediaImage-{{image.alt}}" src="{{ image | img_url: 'large' }}" alt="{{ image.alt | escape }}">
            </button>
          {% endif %}
        {% endunless %}
      {% endfor %}
    </div>
    <div class="productpage__media-mobile-image-container">
      {% for image in product.images %}
        {% unless image.attached_to_variant?%}
          {%if image.width > 100 %}
            <button class="productpage__media-mobile-image-button" id="mediaPageImageButton-{{image.alt}}-{{product.handle}}">
              <img class="productpage__media-mobile-image" id="productPageMediaImage-{{image.alt}}" src="{{ image | img_url: 'large' }}" alt="{{ image.alt | escape }}">
            </button>
          {% endif %}
        {% endunless %}
      {% endfor %}
    </div>
  </div>
  {{ 'media.js' | asset_url | script_tag }}
</div>
